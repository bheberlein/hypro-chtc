name: htconda
channels:
  - conda-forge
dependencies:
  - python >= 3.8
  - gdal >= 3.3
  - numpy >= 1.20, <1.23
  - matplotlib >= 3.3
  - pandas >= 1.2
  - scipy >= 1.7
  - numba
  - shapely
  - arosics
