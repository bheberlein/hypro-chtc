name: htconda
channels:
  - conda-forge
dependencies:
  - python >= 3.8, < 3.10 | > 3.11
  - gdal >= 3.3
  - numpy >= 1.20
  - matplotlib
  - pandas
  - scipy >= 1.7
  - numba
  - shapely
  - arosics
  - lxml
  - pip
